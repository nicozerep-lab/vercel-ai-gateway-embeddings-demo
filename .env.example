# Copy this file to .env.local (which is git-ignored) and fill in values
# with your own credentials. Never commit real secrets to the repository.

# Required database connection string for Drizzle and Neon.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Add any other API keys or tokens your deployment requires as needed.
# For example:
# VERCEL_AI_GATEWAY_URL="https://gateway.ai.vercel.com/api/v1"
# VERCEL_AI_GATEWAY_TOKEN="your-token"
